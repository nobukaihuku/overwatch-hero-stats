# 修正記録: hero-stats collector に「QPフォールバック縮退」ガードを追加 (2026-07-19)

対象: `fetch-hero-stats.mjs`(このリポジトリ)/ サイト側 `src/lib/hero-stats.ts`(別リポジトリ・対応済み)

前回の修正記録: `hero-stats-collector-fix-2026-07-05.md`(SSRスクレイプ → JSONエンドポイント切替 + ティア軸の縮退検知)

---

## 1. 症状

公式 rates API (`https://overwatch.blizzard.com/ja-jp/rates/data/`) は、**日によって `rq` / `map`
フィルタを適用せず、クイック・プレイ (`rq=0`) の既定ビューを返す**。

2026-07-19 にサイト側で実施した実測(canonical view = Asia / rq=2 / all-maps / PC / tier=All、ana):

| 日付 | ana win / pick | 判定 |
|---|---|---|
| 07-05, 07-06 | 48.9 / 43.3 | 正常(ランク) |
| 07-07, 07-08 | 46.3 / 37.7 | **縮退** |
| 07-11, 07-12 | 48.9 / 42.7 | 正常 |
| 07-14 | 46.4 / 37.7 | **縮退** |
| 07-15 | 48.9 / 42.6 | 正常 |
| 07-16 | 46.4 / 37.7 | **縮退** |
| 07-17 | 48.0 / 45.5 | **縮退** |
| 07-18 | 51.2 / 47.1 | 正常 |

**11日中5日(約45%)が縮退。** 恒久的な仕様変更ではなく、日によって出たり出なかったりする。

決定的な証拠: 正常日の `rq=0` (QP) は ana 37.6 / kiriko 45。縮退日の `rq=2`「ランク」の値は
37.7 / 45 とほぼ同一。つまり**縮退日の「ランクデータ」の正体はクイック・プレイのデータ**。

---

## 2. 検知方法

縮退日には次の3点が同時に起きる:

1. `rq=2` の全 1488 フィルタで **BAN率が全て空**(BANはランクでのみ発生する)
2. **per-map が全滅** — 30マップすべてが `all-maps` と完全同値
3. `rq=2` の中身が **`rq=0` と完全一致**

このうち (1) と (3) をガードの判定に使った((2) は (3) の帰結なので冗長)。

- **(1) BAN率の欠落**: `rq=2` スナップショットのうち BAN率を1つでも持つものが**半数割れ**なら縮退。
  半数を閾値にしたのは、単発の取得失敗による部分欠損と区別するため。
- **(3) rq 軸の一致**: 同じ `input/map/region/tier` で `rq=2` と `rq=0` の heroes 署名が一致したら、
  `rq` が効いていない。

---

## 3. なぜ既存ガードで防げなかったか

2026-07-05 に入れた `assertTierAxisNotCollapsed` は**ティア軸しか見ていない**。
縮退時も**ティア軸は生きている**(8ティアすべて別値が返る)ため、素通りしていた。

さらに厄介なことに、縮退データは**ロール別ピック率合計が厳密に Tank 100 / Damage 200 / Support 200
で成立する整合したデータ**である。壊れたデータではなく「正しいQPデータが誤ったラベルで保存されている」
状態なので、`slugCount===0` / `slugCount!==rowCount` / 失敗率閾値といった後段の異常検知に一切引っかからない。

**教訓**: 「値が壊れているか」ではなく「**要求した軸が返り値に反映されているか**」を検査する必要がある。
今回のガードも前回のガードも、この形をしている。

---

## 4. 影響

サイトの `/stats/` は「直前スナップショットとの勝率差」を急上昇/急降下 TOP3 として表示する。
正常日と縮退日をまたぐと、**実際には動いていないヒーローが大きな delta として出る**。

実測では **Torbjorn ▼8.2pt** という架空の変動が表示されていた
(サイト側の防御フィルタ適用後の正しい値は最大でも ▼1.9pt)。

なお 2026-07-12 に入れた重複排除は完全一致のみを除去する設計で、これは正しく動作している。
縮退日は前日と別データなので当然素通りする。**重複排除側の変更は不要**(今回も変更していない)。

### 汚染日

`hero-stats/2026-07-07.json` / `2026-07-08` / `2026-07-14` / `2026-07-16` / `2026-07-17`

---

## 5. 対応

### collector 側(このリポジトリ)

`fetch-hero-stats.mjs` に `assertRankedAxisNotCollapsed(snapshots)` を追加し、
既存の `assertTierAxisNotCollapsed(snapshots)` の**直後**(取得完了後・payload 組み立て前)で呼ぶ。
縮退を検知したらその日は**保存せず `process.exit(1)`** で中断する。

```js
  assertTierAxisNotCollapsed(snapshots);
  assertRankedAxisNotCollapsed(snapshots);
```

これにより、**日付の欠落は「データ未変化」「収集失敗」に加えて「縮退日」も意味する**ようになった
(どれなのかは Actions ログで区別)。README にも追記済み。

### サイト側(別リポジトリ・二重防御)

`src/lib/hero-stats.ts` に `isDegenerate()` を追加。BAN率が半数割れのファイルを系列構築時に除外し、
除外件数をビルドログに warn 出力する。`delta()` のロジックは変更なし(元々正しく、入力が汚れていただけ)。

collector 側の修正が入っても**サイト側の防御は残す**。既に保存済みの汚染5日分に対しては
サイト側フィルタだけが効くため、二重防御の片側として必要。

効果: `/stats/` の変動表示が Torbjorn ▼8.2pt などの架空値から、実勝率変動として妥当な範囲
(最大 ▼1.9pt)に是正された。

---

## 6. 検証

フル取得(1674リクエスト・約30分)は検証目的では**実行しない**。
保存済みスナップショットに対してガードをリプレイする方式で検証した(ネットワーク不要)。

```
node --check fetch-hero-stats.mjs
```

リプレイ結果(`hero-stats/` の全ファイル + origin/main の 07-14〜07-18):

```
2026-06-07 PASS   ← rq=2 未収集の旧ファイル。検査対象外なので通過が正しい
2026-06-10 PASS
2026-06-17 PASS
2026-06-29 PASS
2026-07-03 PASS
2026-07-05 PASS   ← 正常なランクデータ
2026-07-06 PASS
2026-07-07 BLOCK  ← 縮退
2026-07-08 BLOCK  ← 縮退
2026-07-11 PASS
2026-07-12 PASS
2026-07-14 BLOCK  ← 縮退
2026-07-15 PASS
2026-07-16 BLOCK  ← 縮退
2026-07-17 BLOCK  ← 縮退
2026-07-18 PASS
```

期待どおり、縮退5日のみ BLOCK。

---

## 7. 判断事項(2026-07-19 決定済み)

### 7-1. 既存の汚染5ファイルを削除するか → **決定済み (2026-07-19): 削除せず保持**

前例(2026-07-12)ではティア縮退の汚染5件をツリーから削除した。**今回は事情が違う**:

- 縮退ファイルに入っている `rq=0` (QP) のデータは**本物**。正常日の `rq=0` が ana 37.6、
  縮退日の `rq=0` が 37.7 とほぼ一致することを確認済み。QP軸は6月から続く連続資産。
- 壊れているのは「`rq=2` と per-map のラベル」だけ。

**決定: 削除せず保持。** サイト側 `isDegenerate()` が既に系列から除外するため表示上の実害はなく、
削除するとQPの時系列に5日分の穴が空く。7-2 で「QP時系列の穴を避ける」方針を採ったこととも一貫する。

したがって前例 (2026-07-12 のティア縮退5件はツリーから削除) とは**異なる扱いになっている**。
今後この5日を見たときに「なぜ削除されていないのか」と混乱しないよう、ここに理由を残す:
**ティア縮退のファイルは全軸が使い物にならなかったが、今回のファイルは `rq=0` が丸ごと有効**。

### 7-2. 縮退日にQPデータだけでも保存するか → **決定済み (2026-07-19): 現行の中断方式で運用**

現在の実装は縮退を検知したら**丸ごと中断**する。単純で安全だが、その日の本物のQPデータも保存されない。

代案: 「`rq=2` と per-map のスナップショットだけ捨て、`rq=0` を `degenerate: true` フラグ付きで保存する」。
ただし payload の形が変わりサイト側にも対応が要る。

**決定: まずは現行の中断方式で運用する。** QP時系列の穴が実際に問題になった時点で上記の代案を検討する。
判断材料としては、縮退の発生頻度(実測で約4割)が続くか、QP軸を使う表示をサイト側で増やすかどうか。
